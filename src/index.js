/**
 * Adds two numbers
 * @param {number} num1
 * @param {number} num2
 * @returns {Number}
 */
export function add(num1, num2) {
  return num1 + num2;
}

/**
 * Colors Enum
 * @enum {Symbol}
 */
export const Colors = {
  Red: Symbol('Red'),
  Green: Symbol('Green'),
  Blue: Symbol('Blue'),
};
