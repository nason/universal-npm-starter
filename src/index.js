/**
 * Adds two numbers
 * @param {number} a
 * @param {number} b
 */
export function add(a, b) {
  return a + b;
}

/**
 * Colors Enum
 * @enum {Symbol}
 */
export const Colors = {
  Red: Symbol('Red'),
  Green: Symbol('Green'),
  Blue: Symbol('Blue'),
};
